export class Opening {
  constructor(public name: string, public category: string, public moves: string, public gamesFound: number, public win: number, public draw: number, public loss: number) { }
}
